This is my GVim configuration.

You can put the "_vimrc" file in your user directory.

For example, 
1)in win7 system, put the file in the "C:\Users\yourname";
2)in Linux system, rename the file to ".vimrc", then put the file in the "~/.vimrc"
